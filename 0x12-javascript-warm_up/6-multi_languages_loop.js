#!/usr/bin/node
// Printing 3 lines but using string and array
const lines = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}
